///////   9. Palindrome Number   ////////////

class Solution {
public:
    bool isPalindrome(int x) {
        string s= to_string(x);
        string r=s;
        reverse(s.begin(),s.end());
        return s==r;
        
    }
};

/// Second approach is with reminder 
